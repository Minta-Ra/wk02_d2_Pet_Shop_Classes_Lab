class Pet:
    def __init__(self, name, type, breed, price):
        # We use self. so we can refer it to somewhere else
        self.name = name
        self.type = type
        self.breed = breed
        self.price = price
